#ifndef GLM_NATIVE_H
#define GLM_NATIVE_H

#define GLM_NATIVE_VERSION "0.1.0"

/*
 * First milestone for a DS4-style GLM native engine.
 *
 * This is intentionally an engine skeleton, not a complete inference engine
 * yet. It establishes the native boundary, loads the GLM GGUF directly, and
 * exposes DS4-style CLI/server surfaces before kernels are written.
 */

#endif
